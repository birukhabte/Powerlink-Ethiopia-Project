/**
 * Admin Chat Page — uses the shared UnifiedChat component
 */
import React from 'react';
import UnifiedChat from '../../components/UnifiedChat';

const Chat = () => <UnifiedChat />;

export default Chat;
